# Time Jumper

This folder contains a minimal SpriteKit template for the "Time Jumper" iOS game idea. It includes a basic scene that spawns portals and lets the player tap the screen to jump between them.

To use it, create a new Xcode project (Game template with SpriteKit) and replace the generated source files with the ones in `Sources/`.
